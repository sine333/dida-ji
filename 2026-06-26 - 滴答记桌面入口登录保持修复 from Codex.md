# 2026-06-26 - 滴答记桌面入口登录保持修复 from Codex

文件名：版本小结/2026-06-26 - 滴答记桌面入口登录保持修复 from Codex.md
作者：from Codex
日期：2026-06-26

---

## 问题

- 邮箱 magic link 可以正常进入滴答记。
- 但把网页保存到手机桌面后，从桌面入口打开时又要求重新填写邮箱登录。
- 这通常发生在手机把浏览器页面和桌面 WebApp 当成两个启动容器时，桌面入口没有直接继承浏览器 tab 里的 localStorage 会话。

## 实际改动

- `dida-ji.html`
  - 登录会话继续保存到 localStorage。
  - 新增同源 cookie 兜底保存，提升桌面 WebApp 入口继承登录态的成功率。
  - 页面启动时优先读取 localStorage，如果没有会话，再尝试从 cookie 恢复。
  - 退出登录时同时清除 localStorage 和 cookie。
  - 会话只保存必要字段：access token、refresh token、过期时间、用户 id 和邮箱。

- `sw.js`
  - 缓存版本从 `dida-ji-cache-v7` 升到 `dida-ji-cache-v8`。
  - service worker 只缓存本应用同源文件。
  - Supabase 登录、用户信息和云端记录接口不再进入 PWA 离线缓存，避免旧认证或旧数据被误用。

## 手机端验证建议

- 部署 `dida-ji.html` 和 `sw.js` 后，先在手机浏览器里重新打开线上地址一次，让新版 service worker 接管。
- 通过邮箱链接完成一次登录。
- 再从手机桌面入口打开，检查是否直接进入已登录状态。
- 如果某个手机系统仍完全隔离浏览器和桌面 WebApp 的存储，那么第一次从桌面入口打开可能仍需登录一次；完成后，桌面入口自己的登录态会被保留。

## 待部署文件

- `dida-ji.html`
- `sw.js`
- `版本小结/2026-06-26 - 滴答记桌面入口登录保持修复 from Codex.md`

---

## v2026-06-26-desktop-otp-login

### 追加问题

- 仅靠 magic link 仍不够可靠。
- 如果手机把邮箱链接打开到外部浏览器，原来添加到手机桌面的 WebApp 仍然没有登录态。
- 对用户来说，真正需要的是“从原来的桌面图标进入，也能完成登录”。

### 追加改动

- `dida-ji.html`
  - 登录面板新增验证码输入框。
  - 用户从桌面图标打开滴答记后，可以输入邮箱、发送邮件、把邮件里的验证码填回当前桌面 App。
  - 新增 `/auth/v1/verify` 验证逻辑，验证码正确后直接把 Supabase session 写入当前桌面 App。
  - 新增验证码清洗逻辑，复制带空格的验证码时自动整理。
  - 登录提示从“点邮箱链接”改成“把验证码填回这里”。

- `sw.js`
  - 缓存版本从 `dida-ji-cache-v8` 升到 `dida-ji-cache-v9`。

### 必要配置

- Supabase 邮件模板必须显示验证码字段。
- 如果邮件模板里只有 magic link，用户仍会被引导到外部浏览器，桌面 App 内登录就无法完成。

### 更新后的手机端验证建议

- 部署新版 `dida-ji.html` 和 `sw.js`。
- 在手机上从原来的桌面图标打开滴答记。
- 点右上角水滴。
- 输入邮箱并发送邮件。
- 不要点邮件里的登录链接；复制邮件里的验证码，回到桌面 App 输入验证码并登录。
- 登录成功后，再次从桌面图标打开，应直接保持登录。
